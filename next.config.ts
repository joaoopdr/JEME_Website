import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // SVG files served from /public are referenced directly as <img src>
  // No additional image domains needed for local assets
};

export default nextConfig;
